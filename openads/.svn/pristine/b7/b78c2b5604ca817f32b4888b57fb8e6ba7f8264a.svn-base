
  // Page nouveau dossier 

  .demande_autorisation_contestee_hidden_bloc {
    display: none;
  }

  #fieldset-form-demande_nouveau_dossier-type-de-dossier-demande {
    .field:not([class$=field-type-hidden]):not([class$=field-type-hiddendate]):not(.field-type-previsualiser_pdf):not([class*=field-type-html]) {
      grid-template-columns: 275px 1fr;
    }
  }

  //Saisie numero de dossier 

  .bloc_activ_num_manu.saisie_manuelle {
    padding-left: 1em;
    margin: 0;
    .field:not([class$=field-type-hidden]):not([class$=field-type-hiddendate]):not(.field-type-previsualiser_pdf):not([class*=field-type-html]) {
      grid-template-columns: auto 1fr;
    }
  }
  #fieldset-form-demande_nouveau_dossier-numero-de-dossier {
    .fieldsetContent {
      flex-direction: row;
    }
    .bloc {
      display: inline-block
    }
    .field:not([class$=field-type-hidden]):not([class$=field-type-hiddendate]):not(.field-type-previsualiser_pdf):not([class*=field-type-html]) {
      display: inline-block
    }
  }



  //Références cadastrales

  #morelineReferenceCadastrale span {
    @include buttonSecondary;
  }

  .reference_cadastrale_custom_fields {
    align-items: center;

    input[type="text"].reference_cadastrale_custom_field {
      width: 5em;
      margin: .5em 1em .5em 0;
    }

    .moreFieldReferenceCadastrale {
      @include buttonPrimary;
      margin: .5em 0;

      .add-16 {
        padding: 0;
      }
    }
  }
  .recupAdresseButton {
    grid-column: 1/-1;
    .form-libelle {
      visibility: hidden;
    }
  }




  // overlay ajout pétitionnaire  

  [aria-labelledby*="ui-dialog-title-sousform-"]:not([aria-labelledby*="donnees_techniques"]) {
    #form-content {
    display: grid;
      gap: 1em;
    }
    &[aria-labelledby*="petitionnaire"] {
      grid-template-columns: repeat(2, 1fr)
    }

    // Vider le formulaire 
    .delete-16 {
      @include buttonDanger;
    }

    //#fieldset-sousform-petitionnaire-qualite
    //[id*="fieldset-sousform-"][id*="-qualite"],
    //[id*="fieldset-sousform-"][id*="-qualite"]{
    .nolabel.bloc {
      .field {
        grid-template-columns: 1fr;
      }
    }

    fieldset:not(#fieldset-sousform-petitionnaire-qualite, #fieldset-sousform-petitionnaire-collectivite),
    .bloc.particulier_fields,
    .bloc.personne_morale_fields {
      grid-column: 1 /-1;
    }
  }

  .search-frequent-16 {
    @include buttonSecondary;
    align-self: baseline;
    margin-left: 22%;
    font-size: .9em;

    &:before {
      font-size: 1em;
    }

    &::after {
      display: inline-block;
      content: attr(title);
    }
  }
  // Récépissé 

  //[class*="col_"] {



// Demandeurs (aussi valables dans onglets Instructions)

// Button add pétitionnaire principal 

#liste_demandeur {
  .add-16[id*="add_"] {
    @include buttonPrimary;
  }
}

 .synthese_demandeur, 
 form #plaignant_contrevenant .synthese_demandeur {
  box-shadow: 0 2px 4px 1px rgba($brandDarker, .3);
  border-radius: .5em;
  padding: 1em 1.5em;
  margin: 1em;
  display: inline-flex;
  align-items: center;
 }

 form[name="f1"] .synthese_demandeur,
 form[name="f1"] #plaignant_contrevenant .synthese_demandeur 
  {
  box-shadow: none;
  margin-bottom:  2em;
  padding: 0;

  [onclick*="remove"] {
    border-radius: .25em;
    padding: .5em;
    margin-right: .5em;
    background: $danger;
    color: $blanc;

    &:hover {
      background-color: $rouge;
    }
  }

  .edit_demandeur {
    position: relative;
    box-shadow: 0 2px 4px 1px rgba($brandDarker, .3);
    border-radius: .5em;
    padding: 1em 1.5em;
    margin: 1em;
    &::after {
      position: absolute;
      top: 0;
      left: 0;
      display: flex;
      content: "MODIFIER";
      border-radius: .5em;
      width: 100%;
      height: 100%;
      font-weight: 700;
      color: transparent;
      background-color: transparent;
      justify-content: center;
      align-items: center;
      transition: all 0.2s;
    }
    &:hover {
      &:after {
        background-color: rgba($brandDark, .95);
      color: $blanc;
      }
    }
  }

}



.legend_synthese_demandeur {
  margin-top: 1em;
  font-size: 1.1em;
}

#plaignant_contrevenant {
  display: flex;
  gap: 20%;
}

.mainrubrik-guichet-unique {

  form:not(#affichage_reglementaire_attestation_form):not(#advanced-form) #form-content>.field {
    grid-column: 1 / -1;
    grid-template-columns: 1fr 1fr;
  }

  .field-type-autorisation_contestee {
    .form-content {
      display: flex;
      .ui-button {
        margin: 0;
        flex-shrink: 2;
      }
      #autorisation_contestee_cancel_button {
        @include buttonDanger;
        margin: 0;
      }
    }
  }

  .bloc.col_12.localisation #fieldset-form-demande_nouveau_dossier-localisation .fieldsetContent{
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      .references_cadastrales_new_field,
      #morelineReferenceCadastrale {
        grid-column: 1/-1;
      }


  }    .bloc.col_12.localisation #fieldset-form-demande_dossier_encours-localisation .fieldsetContent{
    .moreFieldReferenceCadastrale {
      display: none;
    }


  }                                                                                                     
  

}